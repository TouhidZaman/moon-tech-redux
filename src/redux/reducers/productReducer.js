const initialState = {
    test: "Redux setup successful"
}

export const productReducer = (state = initialState, action) => {
    switch(action.type) {
        default: 
            return state
    }
}